#secuencia de objetos ordenados no modificables
tupla = (1, 3, 2, 5, 1, 2, 5)
#para eliminar elementos repetidos lo puedo convertir en un set, 
#un set es un conjunto que no amite repeticiones

tupla = set(tupla)
tupla = list(tupla)
print(tupla)